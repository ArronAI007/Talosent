# Scripts

Developer entry points for common workflows.

| Script | Purpose |
| --- | --- |
| `test.sh` | Run unit + integration tests (`--all` includes e2e, `--cov` adds coverage) |
| `dev.sh` | Bootstrap a dev environment (editable install + dev dependencies) |

Keep scripts POSIX-friendly, executable (`chmod +x`), and free of secrets.
